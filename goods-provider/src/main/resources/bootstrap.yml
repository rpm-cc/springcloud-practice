
spring:
  cloud:
    config:
      name: goods
      profile: dev
      label: master
      discovery:
        service-id: service-config
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
